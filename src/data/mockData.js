/**
 * VoiceGuard AI - Core data defaults in standard JavaScript
 * All sample/mock scan records removed so only genuine original user data is saved.
 */

export const INITIAL_SCANS = [];

export const SAMPLES_DATA = [];

export const DEFAULT_SETTINGS = {
  liveMonitoring: true,
  instantAlerts: true,
  uploadScanNotify: true,
  weeklyThreatSummary: false,
  sensitivity: 85,
  highPriorityThreshold: 90,
};
